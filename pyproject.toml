[project]
name = "histogram-retriever"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "fran-felix", email = "franastico@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.2",
    "pillow>=12.3.0",
    "pip>=26.2.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
histogram-retriever = "histogram_retriever:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
